package javaCode;

import org.junit.Assert;
import org.junit.Test;
import static org.junit.jupiter.api.Assertions.assertAll;

import static org.junit.Assert.*;
import static org.junit.jupiter.api.Assertions.assertThrows;
/*
public class ValidatorTest {

    @Test
    public void name(){
        //Correct names
        assertEquals(Validator.name("Trym"), "Trym");
        assertNotNull(Validator.name("Ole-Henrik"), "Ole-Henrik");

        //Incorrect names
        assertNotEquals(Validator.name("mail@mail.no"), "mail@mail.no");
        assertNotEquals(Validator.name("@ @ @ @ "), "@ @ @ @ ");


    }

    @Test
    public void phone() {
        //Correct numbers
        assertEquals(Validator.phone("98086042"), "98086042");

        //Incorrect numbers
        assertEquals(Validator.phone("Hei"), "Hei");
    }

    @Test
    public void email() {
        //Correct email
        assertEquals(Validator.email("example@example.com"), "example@example.com");
        assertEquals(Validator.email("uk@domain.co.uk"), "uk@domain.co.uk");
        assertEquals(Validator.email("epost@mail.net"), "epost@mail.net");
        assertEquals(Validator.email("stenberg.trym@gmail.com"), "stenberg.trym@gmail.com");
        assertEquals(Validator.email("s341827@oslomet.no"), "s341827@oslomet.no");

        //Incorrect email
        assertNotEquals(Validator.email(""), "");
        assertNotEquals(Validator.email("trym"), "trym");
        assertNotEquals(Validator.email("@gmail.com"), "@gmail.com");
        assertNotEquals(Validator.email("trym@post"), "trym@post");
        assertNotEquals(Validator.email("test@"), "test@");
    }
}

 */